# frozen_string_literal: true

module CommunitiesHelper
  # def join?
  #   CommunityUser.where(user_id: current_user.id).ids.present?
  # 	# ids = community_users.where(user_id: user.id).where(community_id: self.id)
  # 	# ids.present?
  # end
end
